# 拼版多版面：延後事項與已定案的判斷

**完成日期：** 2026-09-14 ～ 2026-09-15
**範圍：** 拖曳上傳、多版面自動拼版、版面尺寸清單、多版面匯出
**相關 PR：** #3（CI 自動部署）、#5（拖曳上傳）、#7（多版面核心）、#8（Blob 失效修正）、#9（版面尺寸清單）、#10（多版面匯出）
**規格：** [`docs/superpowers/specs/2026-09-14-imposition-multi-sheet-design.md`](../superpowers/specs/2026-09-14-imposition-multi-sheet-design.md)

這份文件記錄**刻意沒做的事**與**已經量過、不該重新猜測的判斷**。目的是讓之後接手的人（包含未來的我）不必重新推導，也不會把「已評估後決定不做」誤認為「漏掉了」。

---

## 一、延後未做的三件事

使用者在 2026-09-15 確認這三件**都先放著**。

### 1. 設定頁改版面尺寸後，拼版頁不提示重新排圖

在設定頁把某個尺寸的寬高改掉之後，拼版頁的畫布仍以**舊尺寸**繪製、分頁標籤仍顯示舊的 `sizeName`，而且沒有任何「請按排圖」的提示。

- 只有拼版頁的尺寸勾選（`state.ts` 的 `toggleSheetSize`）會設定 `LAYOUT_STALE_MESSAGE`。
- 這是**既有的不一致**而非本次引入：改「最小間距」同樣不設 stale 訊息。
- 修法需要讓設定層的變動反映到拼版層，屬跨層改動。

**歷史**：階段 3 的整枝審查提出此項並明確延到階段 4，但階段 4 的計畫本身沒有納入，因此至今未做。這是計畫的缺口，不是遺忘。

### 2. 拆分 `src/components/ImpositionCanvas.tsx`

該檔 405 行、單一 forwardRef 元件本體 **172 行**（專案慣例：函式 <50 行）。階段 4 讓它從 114 行成長到 172 行。

建議切兩刀：

- `ItemContent`／`ImpositionItem`／`SheetBoard`／`contentTransform` → `src/components/SheetBoard.tsx`
- `nextPaint`／`captureSheet`／`buildSheetsPdf`／`PDF_DPI`／`MAX_CANVAS_PX` → `src/imposition/pdfCapture.ts`

第二刀有額外好處：`src/imposition/**/*.ts` 在覆蓋率白名單內，而 `captureSheet` 的 scale 計算與 `Number.isFinite` 防護目前**完全沒有單元測試**，搬過去就測得到。拆完後 render 本體仍約 120 行；要再降得把 `exportPDF` 抽成 `useSheetPdfExport` hook。

**為何延後**：這兩刀會動到剛以變異測試逐條驗證過的程式碼，與功能修正分開做比較安全。

### 3. 抽掉 e2e 重複的前置設定

`e2e/cut-underprint-imposition.spec.ts` 有三處逐字重複約 20 行的多版面前置（進設定頁 → 新增小尺寸 → 回拼版頁 → 取消勾選 7 個預設尺寸 → 填份數 → 排圖），抽成 helper 可少約 40 行。

⚠️ 抽的時候 **`DEFAULT_SIZE_NAMES` 要維持硬編碼**，不要改成 `import { DEFAULT_SHEET_SIZES }`。改了會變成用實作驗證實作：若某個預設尺寸的核取方塊 testid 拼錯或沒渲染，名單會自動同步而測不出差異。硬編碼在預設清單異動時失敗，那是有意義的提醒。

---

## 二、已定案的效能數字（勿重新猜測）

PDF 多頁匯出，本機 headless chromium 實測：

| 情境 | 10 張版面 | 50 張版面 |
|---|---|---|
| 小圖（400×240 px） | 1,176 ms | 7,125 ms |
| 大圖（2400×1600 px，每張版面掛一件全解析度圖） | 2,322 ms | 12,835 ms |

耗時**呈線性**（每張 118–257 ms）。計畫設定的門檻是「10 張超過 30 秒才需提出」，實測差兩個數量級。

**結論：不改掛載策略（匯出時一次把所有版面掛上畫面外暫存區）、不加版面數上限**（維持既有 `MAX_SHEETS = 50`）。

**這組數字沒有涵蓋的**：記憶體峰值（只量了牆鐘時間）、單張版面塞滿多件大圖、行動裝置或低記憶體機器。

若日後有人主張改成「分批掛載」以降低尖峰記憶體——**請先量出數字再改**。當初審查建議分批時沒有數字，量完才發現沒有必要。

---

## 三、覆蓋率百分比看不出的盲點

- 階段 4 新增的 `SheetBoard`、PDF 畫面外暫存區、`captureSheet`、`buildSheetsPdf` **全在 `src/components/`，不在覆蓋率白名單內**。它們的保障 100% 來自 e2e，而不是那個 94.49% 的全域數字。
- 「有被執行但沒有被斷言」的程式碼會讓覆蓋率好看卻毫無保障。`sheetsWithContent` 的 `layerIds.has(...)` 一度如此——把它改成恆真，292 個單元測試沒有任何一個會失敗（後來補了直接測試）。

判斷保障程度時，**用變異測試（把實作改壞，確認恰好只有對應測試失敗）比看覆蓋率百分比可靠**。本次所有關鍵防護都以此方式驗證過。

---

## 四、只能靠人眼確認的 PDF 行為

以下五項 e2e 涵蓋不到（需要 PDF parser 或肉眼），**尚未驗證**：

1. PDF 頁數是否等於版面數
2. 每頁紙張尺寸是否符合該版面（在閱讀器看文件屬性）
3. 匯出過程畫面是否閃爍或跳動
4. 畫布縮放改成 25% 再匯出，解析度是否與 100% 時相同
5. 匯出期間 Toast 是否顯示「正在產生 PDF：N / M 頁」

e2e 已涵蓋的是：下載確實觸發、檔名為 `imposition-layout.pdf`、畫面外暫存區在匯出結束後從 DOM 移除、連點兩次只產生一次下載、匯出途中切換分頁仍會完成。

---

## 五、接受並記錄的結構性殘留（非缺陷）

- **舊 localStorage 裡的重名尺寸擋不掉**。UI 有兩條唯一化路徑（新增自動避名、改名撞名還原），但只擋**新產生**的重名。若使用者的設定從舊版或手動編輯而來、已含重名，`ImpositionPanel` 會出現重複 React key 與重複 testid，且兩者共用同一個勾選狀態（`disabledSizeNames` 以名稱為鍵）。
  **刻意如此**：若在 `sheetSizeSchema` 加唯一性約束，一份含重名的既存設定會讓 `parseSettings` 整包失敗 → 退回預設 → **使用者的 remove.bg API key 被無聲清空**。保住 API key 比擋掉這個邊緣情況重要。

- **刪除某尺寸後再新增同名尺寸，會沿用舊的停用狀態**。`uniqueNewSheetSizeName` 只看設定清單，看不到拼版頁的 `disabledSizeNames`，兩層刻意解耦。使用者一鍵可勾回，且已補上「全部停用時顯示原因」的提示，不會再出現「排圖是灰的卻不說為什麼」。

- **空版面時版面框位置略為下移**。空狀態提示從版面框內搬到框外（必要改動——否則「尚無排版項目」會被截進 PDF），置中容器因此多了一個子節點。只影響沒有任何項目時的畫面。

---

## 六、尚未決定的專案層級事項

本專案**沒有安裝 `@types/react`**，`tsconfig` 也沒開 `strict`，因此 JSX props 實際上不受型別檢查（已實證：刪掉必要 prop 或傳入錯誤型別都能通過 typecheck）。

實務影響：`key` 不能直接寫在自訂元件上（會報 `Property 'key' does not exist`），全專案統一改用 `<React.Fragment key={...}>` 包一層。

此事已向使用者提出，尚未決定，不主動重提。

---

# 追記：分層匯出（2026-09-15）

使用者回報「匯出不正確」——原本 PDF 是把畫布當下可見的圖層**合成**成一張點陣圖，但印刷實務需要**分層輸出**：原圖、白墨、刀模各自一個檔，同一個圖在各層位置完全一致（套印），多版面時每份各含多頁。

## 白墨的顏色：一個被推翻的設計

我最初主張白墨那份 PDF 應該用**白底黑形狀**，理由是分色版慣例「有墨處為黑」——白色在白底上不可見，而且深灰底會被當成滿版墨印出來。

**使用者推翻了這個方案**：「無論是 PDF 或 SVG，白墨都應該是要完全白色，至於檢視問題，應該是調整呈現的方式，讓使用者好理解。」

最終行為：

- 白墨在 **PDF 與 SVG 都使用設定頁的顏色**（預設 `#FFFFFF`），不做任何顏色覆寫
- **匯出時強制實墨**：`pdfLayerColors` 只覆寫 `underprintOpacity: 1`；畫面即時畫布維持 `DEFAULT_UNDERPRINT_OPACITY`（0.8），讓使用者看穿疊圖
- **檔案本身不動**（不鋪深灰底、不改極性），改在拼版頁的匯出區塊加說明：白墨那份在白底檢視器看起來空白是正確的

不要再把白墨改成黑色。這是使用者的領域判斷，不是疏漏。

## 匯出的檔案結構

- **PDF**：一顆按鈕，依 `kindsWithContent` 產出 `imposition-artwork.pdf`／`imposition-underprint.pdf`／`imposition-cut.pdf`，只輸出有內容的層。原本的合成單檔 `imposition-layout.pdf` 已移除。
- **SVG**：「分層 SVG」從「一個檔三個群組」改為每層每版面各一檔；「只有刀模」「只有白墨」兩顆行為不變。
- `kindsWithContent`（`state.ts`）由 PDF 與 SVG **共用**，避免兩套判斷分岔——這是本專案第三次做同樣的收斂（前兩次是 `sheetsWithContent`、`placedItems`）。

## 一併修掉的三個問題

1. **版面框的虛線邊框與圓角會被截進每一張 PDF**。`SheetBoard` 的 `border-2 border-dashed rounded-lg` 在 `captureSheet` 時沒有被中性化，造成白墨分色版上多一圈假墨、圓角切掉滿版項目，以及全域 `box-sizing: border-box` 下內容內縮約 **0.53 mm**（PDF 與 SVG 之間的系統性偏移）。現在截圖期間會把 `borderWidth`／`borderRadius` 歸零並在 `finally` 還原。**三層之間的套印原本就一致**，這個偏移不影響層間對位。
2. **下載數回報的是「嘗試次數」而非「實際落地數」**。瀏覽器封鎖多檔下載時會靜默丟棄，而分層讓 SVG 一次可能送出 15 個檔、PDF 從 1 檔變 3 檔。偵測不到是否被擋，所以文案改為「已送出 N 個檔案的下載」並提示可能需要允許多檔下載——**不假裝知道實際下載了幾個**。
3. **上游競態會靜默弄丟白墨**。切到 Underprint 後立刻按「送到 Imposition」，白墨幾何還沒算完就會被永久定型成 `underprint: null`，之後分層匯出只產兩份——少的正是要交給印刷廠的那一份。兩個面板的送出按鈕現在都會在幾何未就緒時停用。

### 兩個面板的停用條件**刻意不同**，不要「順手統一」

底層判準共用 `isGeometryPending(status)`（`useGeometry.ts`），但組合方式不同：

- `UnderprintPanel`：裸的 `isGeometryPending(...)`
- `CutlinePanel`：`underprintEnabled && underprintPending`

原因是時序。`underprintVisitedFor` 由 `useEffect` 在切到 Underprint 分頁時設定（`App.tsx:96-98`），`underprintEnabled` 由它推導，所以**剛切過去的那一個 render，`underprintEnabled` 仍是 false**。若 `UnderprintPanel` 也 AND 上去，按鈕會在那個瞬間變成可按，而同一時刻 `sendToImposition` 也會因為 `underprintEnabled` 為 false 而收集到空白墨——正好重新打開這個修正要關掉的洞。`CutlinePanel` 沒有這個剛掛載的時間窗，而 AND 條件是讓「從未開過 Underprint」的使用者不被無關的幾何狀態擋住所必需。

## 效能：分層的代價（已量測，使用者決定接受）

| 版面數 | 分層前（單一合成檔） | 分層後（三個檔） |
|---|---|---|
| 10 張 | 2.3 s | **9.8 s** |
| 50 張 | 12.8 s | **62.5 s** |

拆解（10 張、三層，大圖 2400×1600 px）：

| 階段 | 次數 | 總計 | 平均 |
|---|---|---|---|
| html2canvas 截圖 | 30 | 6,618 ms | 221 ms/次 |
| jsPDF 組檔 | 3 | 5,978 ms | ~200 ms/頁 |

**成本大約對半分在兩個階段，而兩者都被層數乘三倍**——不只截圖變三倍，`buildSheetsPdf` 也從「1 檔 N 頁」變成「3 檔各 N 頁」，這一段先前被合併在總時間裡看不出來。這就是為什麼是 4.9 倍而不是 3 倍：**每張成本還會隨版面數上升**（10 張 221 ms/次、50 張約 417 ms/次），成因未確認（可能是同時掛載的版面數，也可能是大張 data URL 堆積）。

各層成本不同：刀模最快（約 176 ms/張，只有細線），原圖最慢（約 230–300 ms/張，要點陣化整張圖）。

**使用者決定接受現況**，不優化、不加版面數上限。10 張 9.8 秒仍低於計畫訂的 30 秒門檻。若日後要改成分批掛載，這裡已有「每張成本隨版面數上升」的實測依據——但仍請先量再改。
